using UnityEngine;

public class mysterybox : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("what kind of box is this");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
