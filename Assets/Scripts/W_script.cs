using UnityEngine;

public class W_script : MonoBehaviour
{

    [Header("w script")]
    public float w;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        if(1==1)
        {
            Debug.Log("1 = 1");
        }
    }

    // Update is called once per frame
    void Update()
    {
    }
}
