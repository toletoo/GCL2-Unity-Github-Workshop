using UnityEngine;

public class JarelController : MonoBehaviour
{
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("I love gooning");
    }

    // Update is called once per frame
    void Update()
    {
    }
}
